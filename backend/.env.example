# Copy to .env (gitignored) and fill in.
ANTHROPIC_API_KEY=sk-ant-...
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...
SUPABASE_ANON_KEY=eyJ...

# Meta WhatsApp Cloud API (test number — 5 verified recipients)
META_WA_VERIFY_TOKEN=ds-wa-verify-2026
META_WA_PHONE_NUMBER_ID=
META_WA_ACCESS_TOKEN=
META_WA_APP_SECRET=
META_WA_BUSINESS_ACCOUNT_ID=

# DS CRM
DS_CRM_BASE_URL=
DS_CRM_USERNAME=
DS_CRM_PASSWORD=
DS_CRM_JWT=
DS_CRM_USER_ID=
DS_CRM_COMPANY_ID=
DS_CRM_AGENCY_ID=

# Platform accounts (migration 009 + scripts/seed_platform_users.py)
# APP_CRYPTO_KEY: Fernet key used to encrypt per-user CRM passwords at rest.
#   Generate once: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# APP_SERVICE_PIN: global PIN that unlocks the CRM-credentials ("Definições") tab.
APP_CRYPTO_KEY=
APP_SERVICE_PIN=28021904

# Demo recipients (E.164, comma separated) — used by /api/whatsapp/send-demo
DEMO_RECIPIENTS=

# Operational
ENVIRONMENT=development
# Nome da loja desta instância (o código serve várias lojas; muda só o env).
LOJA_NAME=DS Crédito Ramada – Jardim da Amoreira
# Forma curta p/ cabeçalhos/prompts; default = LOJA_NAME até ao " – ".
LOJA_SHORT_NAME=
# Prefixo das instâncias WhatsApp no Evolution PARTILHADO — único por loja
# (ex.: ds_ Ramada, dsl_ Loulé). Nunca repetir entre instâncias.
EVOLUTION_INSTANCE_PREFIX=ds_
BRAND_RED=#E30613

# Frontend (env de BUILD, definir em frontend/.env.local na máquina de deploy):
#   NEXT_PUBLIC_LOJA_NAME="DS Crédito Ramada"
#   NEXT_PUBLIC_LOJA_NAME_FULL="DS Crédito Ramada – Jardim da Amoreira"
